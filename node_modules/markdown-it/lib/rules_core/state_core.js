amis.define('node_modules/markdown-it/lib/rules_core/state_core', function(require, exports, module, define) {

  // Core state object
  //
  'use strict';
  
  var Token = require('node_modules/markdown-it/lib/token');
  
  
  function StateCore(src, md, env) {
    this.src = src;
    this.env = env;
    this.tokens = [];
    this.inlineMode = false;
    this.md = md; // link to parser instance
  }
  
  // re-export Token class to use in core rules
  StateCore.prototype.Token = Token;
  
  
  module.exports = StateCore;
  

});
